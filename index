<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Connexion avec Google</title>

  <!-- Firebase SDK -->
  <script src="https://www.gstatic.com/firebasejs/9.22.2/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.2/firebase-auth-compat.js"></script>
</head>
<body>
  <h2>Connexion avec Google</h2>
  <button id="google-sign-in">Se connecter avec Google</button>

  <script>
    // Remplace ici par tes vraies infos Firebase
    const firebaseConfig = {
      apiKey: "AIzaSyBGxs6sUV2lp5bFaEvdV7fLAz6wz4vwoZI",
      authDomain: "questionnairexd.firebaseapp.com",
      projectId: "questionnairexd"
      appId: "1:673392594508:web:7943a387ba7aa51d96eee9"
    };

    firebase.initializeApp(firebaseConfig);
    const auth = firebase.auth();

    document.getElementById("google-sign-in").addEventListener("click", () => {
      const provider = new firebase.auth.GoogleAuthProvider();
      auth.signInWithPopup(provider)
        .then(result => {
          console.log("Connecté :", result.user.displayName);
          alert("Bienvenue, " + result.user.displayName);
        })
        .catch(error => {
          console.error("Erreur :", error);
          alert("Erreur : " + error.message);
        });
    });
  </script>
</body>
</html>

//////////

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Questionnaire Vocal</title>
  <script src="https://www.gstatic.com/firebasejs/9.22.2/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.2/firebase-auth-compat.js"></script>
</head>
<body>
  <h1>Questionnaire vocal</h1>

  <!-- Connexion Google -->
  <div id="auth-section">
    <p>Merci de vous connecter pour répondre :</p>
    <button onclick="signInWithGoogle()">Se connecter avec Google</button>
  </div>

  <!-- Section question -->
  <div id="question-section" class="hidden">
    <p id="question">Quelle est votre expérience aujourd'hui ?</p>
    <button onclick="startDictation()">🎤 Répondre à l'oral</button>
    <p id="response"></p>
  </div>

  <script>
    // 🧩 Firebase config
    const firebaseConfig = {
      apiKey: "AIzaSyBGxs6sUV2lp5bFaEvdV7fLAz6wz4vwoZI",
      authDomain: "questionnairexd.firebaseapp.com",
      projectId: "questionnairexd"
    };
    firebase.initializeApp(firebaseConfig);
    const auth = firebase.auth();

    // 🟢 Google Sign-In
    function signInWithGoogle() {
      const provider = new firebase.auth.GoogleAuthProvider();
      auth.signInWithPopup(provider)
        .then(result => {
          console.log("Connecté :", result.user.email);
          document.getElementById('auth-section').classList.add('hidden');
          document.getElementById('question-section').classList.remove('hidden');
        })
        .catch(error => {
          console.error("Erreur de connexion :", error);
          alert("Erreur : " + error.message);
        });
    }

    // 🎤 Dictée vocale
    function startDictation() {
      const recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
      recognition.lang = 'fr-FR';
      recognition.start();
      recognition.onresult = (event) => {
        const response = event.results[0][0].transcript;
        document.getElementById('response').textContent = "Réponse : " + response;
        sendToAirtable(response);
      };
      recognition.onerror = (error) => {
      console.error("Erreur de dictée vocale:", error);
      alert("Erreur de dictée vocale.");
      };
    }

    // 🧠 Airtable config
    const airtableApiKey = "patm67mBcEcBXZeED.4c1588d4b1dfa37df6a155af1e77a3c1cad77ddd380ae9d7937c41ad750d26f2";
    const airtableBaseId = "apprjFOnbLySO8spa";
    const airtableTable = "shriT4asLXyKRlFbt"; 
    const questionId = "q1";

    // 📨 Envoi vers Airtable
    function sendToAirtable(text) {
      const user = auth.currentUser;
      if (!user) return alert("Pas connecté");

      fetch(`https://api.airtable.com/v0/${airtableBaseId}/${airtableTable}`, {
        method: "POST",
        headers: {
          Authorization: `Bearer ${airtableApiKey}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          records: [{
            fields: {
              Email: user.email,
              Question: questionId,
              Réponse: text,
              Horodatage: new Date().toISOString()
            }
          }]
        })
      })
      .then(res => res.json())
      .then(data => {
        console.log("Enregistré :", data);
        alert("Réponse enregistrée !");
      })
      .catch(err => {
        console.error("Erreur Airtable :", err);
        alert("Erreur lors de l'envoi.");
      });
    }
  </script>

  <style>
    .hidden { display: none; }
    button { font-size: 1.2em; margin-top: 10px; }
  </style>
</body>
</html>
